import {propTypes as offersListTypes} from '../offers-list/offers-list.props';
import {propTypes as citiesListTypes} from '../cities-list/cities-list.props';

export const propTypes = Object.assign({}, citiesListTypes, offersListTypes);
